# Browser requests use the relative /api path. Vite proxies that path to the
# local API, so no client API URL or browser-side secret is required here.
# For a non-default backend during local development only:
# VITE_API_PROXY_TARGET="http://localhost:3000"
